export default {}

export enum LayoutType {
  main = 'main',
  empty = 'empty'
}

export enum Direction {
  ASC = 'ASC',
  DESC = 'DESC'
}

export enum PageMode {
  view = 'view',
  create = 'create',
  edit = 'edit'
}
